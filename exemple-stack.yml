name: Exemple
odoo:
  version: 16.0
  cmdline: "--workers=2"
  addons:
    -
      type: remote
      origin: https://github.com/OCA/project.git
      branch: "16.0"
    -
      type: local
      path: ./git/16.0/OCA/crm
  dependencies:
    apt: pkg-config pkg-config libxml2-dev libxmlsec1-dev libxmlsec1-openssl
    python: pyopenssl
postgres:
  type: local # or remote
  version: 14
  # type: remote
  # host: host
  # user: user
  # db: db
  # password: password
